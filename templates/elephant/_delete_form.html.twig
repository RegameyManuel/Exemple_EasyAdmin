<form method="post" action="{{ path('app_elephant_delete', {'id': elephant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ elephant.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Elephant{% endblock %}

{% block body %}
    <h1>Elephant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elephant.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ elephant.color }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ elephant.weight }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ elephant.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_elephant_index') }}">back to list</a>

    <a href="{{ path('app_elephant_edit', {'id': elephant.id}) }}">edit</a>

    {{ include('elephant/_delete_form.html.twig') }}
{% endblock %}
